<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="120dp"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Sidebar Panel - готовое изображение меню -->
    <FrameLayout
        android:id="@+id/sidebar_panel"
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:layout_marginStart="4dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp">

        <!-- Готовое изображение меню (одно целое) -->
        <ImageView
            android:id="@+id/sidebar_menu_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:src="@drawable/sidebar_menu" />

        <!-- Кликабельные зоны (вертикально, в порядке как на изображении) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:weightSum="6">

            <!-- 1. Машина -->
            <View
                android:id="@+id/btn_car_sidebar"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true" />

            <!-- 2. Домик (главный экран) -->
            <View
                android:id="@+id/btn_home_sidebar"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true" />

            <!-- 3. Стрелка (навигация) -->
            <View
                android:id="@+id/btn_nav_sidebar"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true" />

            <!-- 4. Нота (музыка) -->
            <View
                android:id="@+id/btn_music_sidebar"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true" />

            <!-- 5. Телефон (звонки) -->
            <View
                android:id="@+id/btn_phone_sidebar"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true" />

            <!-- 6. Квадратики (все приложения) -->
            <View
                android:id="@+id/btn_settings_sidebar"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true" />

        </LinearLayout>

    </FrameLayout>

    <!-- Touch Area for Swipe Gestures -->
    <FrameLayout
        android:id="@+id/touch_area"
        android:layout_width="20dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@android:color/transparent">

        <!-- Handle indicator -->
        <View
            android:id="@+id/sidebar_handle"
            android:layout_width="6dp"
            android:layout_height="100dp"
            android:layout_gravity="center_vertical|start"
            android:layout_marginStart="2dp"
            android:alpha="0.6"
            android:background="@color/accent_cyan"
            android:visibility="gone" />
    </FrameLayout>

</FrameLayout>
